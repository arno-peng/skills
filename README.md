# skills

Personal Cursor skills repository.

## Structure

- `zsh-setup/`: Skill for configuring and troubleshooting zsh themes, plugins, and prompt styles.

## Usage

Place skill folders under `.cursor/skills/` in a project, each containing `SKILL.md`.

## Branch

- Primary working branch: `develop`.
